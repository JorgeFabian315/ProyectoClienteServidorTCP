<UserControl x:Class="ClienteTCP.Views.ConexionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClienteTCP.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center">
        <TextBlock >Escribe la dirección IP del servidor:</TextBlock>
        <TextBox Margin="0,5" Text="{Binding IP}"></TextBox>
        <Button Margin="0,5" Command="{Binding ConectarCommand}" Background="{StaticResource Degradado}">Conectar</Button>
    </StackPanel>
</UserControl>
